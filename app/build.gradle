apply plugin: 'com.android.application'
apply plugin: 'io.fabric'

android {
    compileSdkVersion 28

    defaultConfig {
        applicationId "com.gianlu.aria2app"
        minSdkVersion 21
        targetSdkVersion 28
        versionCode 146
        versionName "4.0.4"
    }

    buildTypes {
        release {
            minifyEnabled true
            shrinkResources true
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }

    flavorDimensions 'main'
    productFlavors {
        standard {
            dimension 'main'
        }
        foss {
            dimension 'main'
        }
    }
}

dependencies {
    api project(':CommonUtils')
    api project(':CountryFlags')
    api 'com.tonyodev.fetch2:fetch2:2.3.0-RC1'
    api 'com.tonyodev.fetch2okhttp:fetch2okhttp:2.3.0-RC1'
    api 'com.squareup.okhttp3:okhttp:3.11.0'
    api 'com.github.PhilJay:MPAndroidChart:v3.1.0-alpha'
    api 'com.getbase:floatingactionbutton:1.10.1'
    api 'org.apmem.tools:layouts:1.10@aar'
    api 'com.llew.huawei:verifier:1.0.6'
}